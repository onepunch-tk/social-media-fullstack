{
  "name": "@social/schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json --resolve-full-paths",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test",
    "dev": "tsc -p tsconfig.build.json --watch & tsc-alias -p tsconfig.build.json --resolve-full-paths --watch"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@social/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "tsc-alias": "^1.9.5",
    "typescript": "~6.0.3"
  }
}
